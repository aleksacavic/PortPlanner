// Geo-ref chip placeholder — Phase 18 implements full UI.

import type { ReactElement } from 'react';

export function StatusBarGeoRefChip(): ReactElement {
  return <span data-component="georef-chip">Not geo-referenced</span>;
}
