// Phase 2 — temporary App. Phase 4 replaces this with the prototype-shell
// (Navbar + Sidebars + CanvasArea + StatusBar) wrapped in ThemeProvider.
export function App() {
  return <h1>PortPlanner</h1>;
}
